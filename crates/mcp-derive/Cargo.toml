[package]
name = "mcp-derive"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "First-party #[tool] proc-macro: JSON Schema generation + dispatch."

[lib]
proc-macro = true

[dependencies]
